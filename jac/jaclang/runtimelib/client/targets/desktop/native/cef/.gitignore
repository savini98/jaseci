# CEF binary distribution + SDK headers - regenerate via desktop_build.fetch_cef_dist
cef_dist/
cef_headers/
# Build artifacts (regenerated by `jac run build.jac` / the CefDesktopTarget pipeline)
libcef_dispatch.so
cef_dispatch.so
cef-subprocess
# Chromium runtime GPU/shader caches (created on first launch)
DawnCache/
GPUCache/
# Local dev spike binaries (not shipped)
test_sizes
test_null_args
.build/
.cef_tarball.tar.bz2
.jac_ir/
.jac/
__pycache__/
.pytest_cache/
