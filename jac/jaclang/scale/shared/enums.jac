enum Platforms {
    GOOGLE = 'google',
    APPLE = 'apple',
    GITHUB = 'github',
    FIREBASE = 'firebase',
}

enum Operations { LOGIN = 'login', REGISTER = 'register', }


enum CredentialType { PASSWORD = 'password', }
