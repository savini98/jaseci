[project]
name = "jac-shop"
version = "0.1.0"
description = "A small e-commerce app built as microservices"
entry-point = "main.jac"

[dependencies.npm]
react = "^18.2.0"
react-dom = "^18.2.0"
react-router-dom = "^6.22.0"
react-error-boundary = "^5.0.0"
zod = "^4.3.6"

[dependencies.npm.dev]
vite = "^6.4.1"
"@vitejs/plugin-react" = "^4.2.1"
typescript = "^5.3.3"
"@types/react" = "^18.2.0"
"@types/react-dom" = "^18.2.0"

[dependencies]
humanize = ">=4.0.0"

[dev-dependencies]
watchdog = ">=3.0.0"

[serve]
base_route_app = "app"

[client]

# The routes table below is the authoritative service cut (#7772): each key
# runs as its own service pod behind the gateway. Everything else
# (database/client entry) stays derived; the e2e harness appends its
# test-only opt-ins (ingress/logs/cors) at run time.

[scale.microservices.routes]
products_app = "/products"
orders_app = "/orders"
cart_app = "/cart"

# --- e2e-harness overlay (appended by k8s_microservice_real_e2e.sh) ---
[scale.microservices.logs]
enabled = true

[scale.microservices.ingress]
enabled = true
host = "jac-shop.local"
ingress_class_name = "nginx"

[scale.microservices.cors]
allow_origins = ["http://app.example.com"]
allow_methods = ["GET", "POST", "OPTIONS"]
allow_headers = ["Authorization", "Content-Type"]
allow_credentials = true
