[project]
name = "day-planner-auth"
version = "1.0.0"
description = "AI Day Planner - authenticated def:priv variant with multi-file frontend"
entry-point = "main.jac"

[dependencies.npm]
react = "^18.2.0"
react-dom = "^18.2.0"
react-router-dom = "^6.22.0"
react-error-boundary = "^5.0.0"
zod = "^4.3.6"

[dependencies.npm.dev]
vite = "^6.4.1"
"@vitejs/plugin-react" = "^4.2.1"
typescript = "^5.3.3"
"@types/react" = "^18.2.0"
"@types/react-dom" = "^18.2.0"

[dev-dependencies]
watchdog = ">=3.0.0"

[serve]
base_route_app = "app"

[client]

[byllm.model]
# default_model = "ollama/gemma4:e4b"
default_model = "local:gemma-4-e2b"

# The entry module is the server app; components call its priv helpers with auth.
[placement.pins]
main = "server"
