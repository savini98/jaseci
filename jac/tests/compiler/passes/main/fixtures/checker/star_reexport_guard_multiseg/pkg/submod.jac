"""Deep submodule provider for the multi-segment star re-export regression."""

import sys;
with entry {
    if (sys.platform != "") {
        class Marker {
            def init(self: Marker, tag: int = 0) -> None {
                self.tag = tag;
            }
        }
    }
}
