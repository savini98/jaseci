- **Fix: corrupted strings in mixed-type lists**: in natively compiled code, a computed string stored into a list that also holds other types (for example `[count, name]`) could read back as garbage or an empty string, or crash the program - it now reads back intact.
