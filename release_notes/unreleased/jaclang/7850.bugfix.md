- **Fix: float/int64 bit-reinterpretation in native-compiled code**: `__float_bits` and `__bits_float` (reinterpret a `float`'s raw bits as an `int` and back) now return the correct bit patterns in native-compiled code instead of silently producing wrong values.
