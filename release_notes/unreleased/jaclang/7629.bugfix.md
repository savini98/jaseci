- **Serializer log de-duplication now covers the schema-drift and alias-resolution paths too**: the info, warning, and error messages the serializer emits while reading anchors are each logged at most once per class per process instead of once per anchor, so a store with schema drift or renamed classes no longer floods the logs.
