- **Fix: `None` string comparisons and imported-enum lowering in native code**: comparing a possibly-`None` string no longer crashes and matches Python's equality semantics, and enums imported from another module now lower correctly regardless of module processing order.
