[project]
name = "js-styling"
version = "1.0.0"
description = "Jac client application: js-styling"
entry-point = "main.jac"

[dependencies.npm]
"jac-client-node" = "1.0.4"

[dependencies.npm.dev]
"@jac-client/dev-deps" = "1.0.0"

[plugins.client]
# Vite bundler configuration (optional overrides)
# vite.plugins = []
# vite.build = {}

# Default packages are automatically included (react, react-dom, react-router-dom, vite, @babel/*, typescript, @types/*, @vitejs/plugin-react)
